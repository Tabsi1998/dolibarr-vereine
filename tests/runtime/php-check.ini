; Loaded by the runtime checks only. Every PHP message is logged to the
; container output, so a warning in module code cannot hide behind the
; production defaults of the Dolibarr image.
error_reporting = E_ALL
log_errors = On
display_errors = Off
